<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>ch.admin.bit.jeap</groupId>
        <artifactId>jeap-doc-service</artifactId>
        <version>3.2.0-SNAPSHOT</version>
    </parent>

    <artifactId>jeap-doc-html</artifactId>
    <name>${project.groupId}:${project.artifactId}</name>

    <!--
        Reading HTML somebody else's build produced, and nothing else.

        This is the only module that may hold an HTML parser. The domain stays free of infrastructure,
        jeap-doc-markdown has no dependencies and must keep none, and jeap-doc-objectstorage is named for what
        it does. Uploaded HTML is the least trustworthy input this service has, so the parser that reads it is
        a dependency of one small module rather than of everything.
    -->
    <dependencies>
        <dependency>
            <groupId>ch.admin.bit.jeap</groupId>
            <artifactId>jeap-doc-domain</artifactId>
        </dependency>
        <dependency>
            <groupId>org.jsoup</groupId>
            <artifactId>jsoup</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-configuration-processor</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- test -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>
