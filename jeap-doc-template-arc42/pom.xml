<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>ch.admin.bit.jeap</groupId>
        <artifactId>jeap-doc-service</artifactId>
        <version>1.2.0-SNAPSHOT</version>
    </parent>

    <artifactId>jeap-doc-template-arc42</artifactId>
    <name>${project.groupId}:${project.artifactId}</name>

    <!--
        The arc42 structure template: its twelve chapters, its structural rules, and the pages the doc service
        generates into it from the architecture model.

        Two jeap-doc dependencies, plus spring-boot-starter for the auto-configuration. Anything else needs a
        reason that is about arc42. A template engine, an HTTP client, a JSON mapper or a dependency on
        jeap-doc-sitegenerator would be a leak: the chapters and the rules are read from the web layer too, for
        the structural validation of an upload, and everything on this POM travels there with them.

        arc42 (https://arc42.org) is by Gernot Starke and Peter Hruschka, licensed CC BY-SA 4.0. See NOTICE in
        the root of this repository for the attribution and for what was changed.
    -->
    <dependencies>
        <dependency>
            <groupId>ch.admin.bit.jeap</groupId>
            <artifactId>jeap-doc-domain</artifactId>
        </dependency>
        <dependency>
            <groupId>ch.admin.bit.jeap</groupId>
            <artifactId>jeap-doc-markdown</artifactId>
        </dependency>
        <dependency>
            <!-- the auto-configuration that registers the template; no Spring beyond that -->
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter</artifactId>
        </dependency>

        <!-- test -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>
