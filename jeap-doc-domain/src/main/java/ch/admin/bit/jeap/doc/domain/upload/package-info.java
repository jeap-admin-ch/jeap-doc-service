/**
 * Everything about an upload: what a bundle says about itself, what happens to it, and what is kept.
 * <p>
 * The descriptor a pipeline sends and the rules it has to satisfy, the service that claims and stores an
 * attempt, the states one moves through, and the housekeeping that removes what is old. The one thing it asks
 * of the rest of the domain is a build: an upload that was stored publishes the site it belongs to.
 */
package ch.admin.bit.jeap.doc.domain.upload;
