/**
 * The documentation a team uploaded, as the doc service holds it.
 * <p>
 * A model of its own, separate from the imported architecture model: nothing links the two, and a generation
 * run joins them per subject. So a system, component or library can be documented before the architecture
 * repository knows it exists, and what is published for it then comes from here alone.
 */
package ch.admin.bit.jeap.doc.domain.custom;
